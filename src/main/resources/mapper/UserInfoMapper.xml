<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.lsmock.mapper.UserInfoMapper">

    <select id="findByUserId" parameterType="com.example.lsmock.dao.UserInfo" resultType="com.example.lsmock.dao.UserInfo">
        select * from user_info WHERE userId=#{userId}
    </select>

</mapper>